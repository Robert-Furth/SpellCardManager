<UserControl x:Class="SpellCardManager.WPFApp.Views.SpellViewer"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:cvt="clr-namespace:SpellCardManager.WPFApp.Converters"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:SpellCardManager.WPFApp.Views"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:mwpf="http://schemas.modernwpf.com/2019"
             xmlns:vm="clr-namespace:SpellCardManager.ViewModels;assembly=SpellCardManager"
             x:Name="root"
             d:Background="White"
             d:DataContext="{d:DesignInstance Type=vm:SpellTabViewModelDesign,
                                              IsDesignTimeCreatable=True}"
             d:DesignHeight="450"
             d:DesignWidth="800"
             mc:Ignorable="d">

    <UserControl.Resources>
        <cvt:ColorToBrushConverter x:Key="colorToBrushConverter" />
        <cvt:DarkOrLightTextConverter x:Key="darkOrLightTextConverter" />
        <BooleanToVisibilityConverter x:Key="booleanToVisibilityConverter" />
    </UserControl.Resources>

    <ScrollViewer Name="OuterScrollView"
                  Padding="15,5,15,0"
                  VerticalScrollBarVisibility="Auto">
        <mwpf:SimpleStackPanel Spacing="10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <TextBlock Style="{DynamicResource TextBlockH1Style}" Text="{Binding Name}" />
                <Button Grid.Column="1"
                        Margin="5,0,0,0"
                        Command="{Binding RelativeSource={RelativeSource AncestorType=local:MainView}, Path=DataContext.EditSpellCommand}"
                        CommandParameter="{Binding Card}">
                    Edit
                </Button>

                <ItemsControl Grid.Column="2" ItemsSource="{Binding Card.SortedTags}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <local:Tag Margin="7,0,0,7"
                                       Foreground="{Binding Color, Converter={StaticResource darkOrLightTextConverter}}"
                                       TagBackground="{Binding Color, Converter={StaticResource colorToBrushConverter}}"
                                       Text="{Binding Name}" />
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </Grid>

            <ListView MinWidth="300"
                      MaxWidth="500"
                      HorizontalAlignment="Left"
                      BorderBrush="{DynamicResource {x:Static SystemColors.ActiveBorderBrushKey}}"
                      BorderThickness="1,0,1,1"
                      ItemsSource="{Binding Card.Attributes}">

                <ListView.Style>
                    <Style TargetType="ListView">
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding Card.Attributes.Count}" Value="0">
                                <Setter Property="Visibility" Value="Collapsed" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </ListView.Style>

                <ListView.ItemContainerStyle>
                    <Style BasedOn="{StaticResource {x:Static GridView.GridViewItemContainerStyleKey}}" TargetType="ListViewItem">
                        <Setter Property="Focusable" Value="False" />
                        <Setter Property="BorderThickness" Value="0,1,0,0" />
                        <Setter Property="BorderBrush" Value="{DynamicResource {x:Static SystemColors.ActiveBorderBrushKey}}" />
                    </Style>
                </ListView.ItemContainerStyle>

                <ListView.View>
                    <GridView>
                        <GridView.ColumnHeaderContainerStyle>
                            <Style TargetType="GridViewColumnHeader">
                                <Setter Property="Visibility" Value="Collapsed" />
                            </Style>
                        </GridView.ColumnHeaderContainerStyle>

                        <GridViewColumn DisplayMemberBinding="{Binding Key}" />
                        <GridViewColumn DisplayMemberBinding="{Binding Value}" />
                    </GridView>
                </ListView.View>
            </ListView>

            <FlowDocumentScrollViewer Name="DescriptionViewer"
                                      Margin="0,0,0,15"
                                      Document="{Binding RenderedMarkdown}"
                                      PreviewMouseWheel="FlowDocumentScrollViewer_PreviewMouseWheel"
                                      VerticalScrollBarVisibility="Hidden" />
        </mwpf:SimpleStackPanel>
    </ScrollViewer>

</UserControl>
