<UserControl x:Class="SpellCardManager.WPFApp.Views.MainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:cvt="clr-namespace:SpellCardManager.WPFApp.Converters"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:dragablz="http://dragablz.net/winfx/xaml/dragablz"
             xmlns:local="clr-namespace:SpellCardManager.WPFApp.Views"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:mwpf="http://schemas.modernwpf.com/2019"
             xmlns:sys="clr-namespace:System;assembly=mscorlib"
             xmlns:vm="clr-namespace:SpellCardManager.ViewModels;assembly=SpellCardManager"
             x:Name="root"
             d:Background="White"
             d:DataContext="{d:DesignInstance Type=vm:MainViewModelDesign,
                                              IsDesignTimeCreatable=True}"
             d:DesignHeight="450"
             d:DesignWidth="800"
             mc:Ignorable="d">
    <UserControl.Resources>
        <sys:Double x:Key="TabWidth">150</sys:Double>
        <cvt:IsItalicConverter x:Key="isItalicConverter" />
    </UserControl.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="250" MinWidth="200" />
            <ColumnDefinition MinWidth="300" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Menu Grid.Row="0"
              Grid.ColumnSpan="2"
              BorderBrush="#FFA0A0A0"
              BorderThickness="0,0,0,1">
            <MenuItem Header="_File" IsSubmenuOpen="False">
                <MenuItem Command="{Binding NewFileCommand}"
                          Header="_New Deck"
                          InputGestureText="Ctrl+Shift+N" />
                <MenuItem Command="{Binding OpenFileCommand}"
                          Header="_Open..."
                          InputGestureText="Ctrl+O" />
                <MenuItem Command="{Binding SaveCurrentFileCommand}"
                          Header="_Save"
                          InputGestureText="Ctrl+S" />
                <MenuItem Command="{Binding SaveAsCommand}"
                          Header="Save _As..."
                          InputGestureText="Ctrl+Shift+S" />
            </MenuItem>
            <MenuItem Header="_Edit">
                <MenuItem Command="{Binding EditNewSpellCommand}"
                          Header="_New Spell..."
                          InputGestureText="Ctrl+N" />
                <MenuItem Command="{Binding EditSelectedSpellCommand}"
                          Header="Edit _Selected Spell..."
                          InputGestureText="Ctrl+E" />
                <MenuItem Command="{Binding EditTagsCommand}"
                          Header="Edit _Tags..."
                          InputGestureText="Ctrl+T" />
            </MenuItem>
        </Menu>

        <local:SearchPanel Grid.Row="1"
                           Grid.Column="0"
                           DataContext="{Binding}" />

        <TabControl Grid.Row="1"
                    Grid.Column="1"
                    ItemsSource="{Binding OpenTabs}"
                    SelectedIndex="{Binding SelectedTab}">
            <TabControl.ItemTemplate>
                <DataTemplate DataType="{x:Type vm:SpellTabViewModel}">
                    <DockPanel Width="{StaticResource TabWidth}" MouseDown="DockPanel_MouseDown">
                        <Button Command="{Binding DataContext.CloseTabCommand, ElementName=root}"
                                CommandParameter="{Binding Mode=OneWay}"
                                DockPanel.Dock="Right"
                                Style="{StaticResource SmallButtonStyle}">
                            <Viewbox>
                                <mwpf:SymbolIcon Symbol="Clear" />
                            </Viewbox>
                        </Button>

                        <TextBlock FontStyle="{Binding IsTemporary, Converter={StaticResource isItalicConverter}}" Text="{Binding Name}" />
                    </DockPanel>
                </DataTemplate>
            </TabControl.ItemTemplate>

            <TabControl.ContentTemplate>
                <DataTemplate>
                    <local:SpellViewer />
                </DataTemplate>
            </TabControl.ContentTemplate>
        </TabControl>

        <GridSplitter Grid.Row="1"
                      Grid.Column="1"
                      Width="2"
                      HorizontalAlignment="Left"
                      Background="Transparent"
                      BorderBrush="#FFA0A0A0"
                      BorderThickness="1,0,0,0" />

    </Grid>
</UserControl>
